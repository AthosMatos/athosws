import styled from "styled-components";

export const ButtonWrapper = styled.a.attrs({
  className: `flex gap-2 p-2 hover:scale-105 
    transition-transform duration-200
    ease-in-out w-fit h-fit rounded-md
    cursor-pointer active:scale-100
    select-none`,
})``;
